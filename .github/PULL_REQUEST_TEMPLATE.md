## What

<!-- The change, in one or two sentences. Link the issue it answers. -->

## Checklist

- [ ] `npm run build && npm run typecheck && npm run lint && npm test` are green
- [ ] Public TSDoc is written for the customer (no internals; every property documented)
- [ ] A changeset is included for any consumer-visible change (`npm run changeset`) — or the change is docs/tooling/test-only
